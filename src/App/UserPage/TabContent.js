import React from 'react';
import styled from '@emotion/styled';

const TabContentComponent = styled.div({
  flexGrow: 1,
  flexShrink: 1,
});

const TabContent = () => <TabContentComponent></TabContentComponent>;

export default TabContent;
