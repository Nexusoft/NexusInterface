import * as TYPE from "../actions/actiontypes";

const initialState = {
  refundAddress: ""
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
